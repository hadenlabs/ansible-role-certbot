# vars file for certbot (Debian specific)

_certbot_packages:
  - python-pip
  - git
  - build-essential
  - libssl-dev
  - libbz2-dev
  - libreadline-dev
  - libsqlite3-dev
  - wget
  - curl
  - llvm
  - python-certbot-nginx

_certbot_pip_dependencies:
  - name: pip
    version: '*'
